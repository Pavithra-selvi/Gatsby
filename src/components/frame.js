import React from "react";
const Frame = ({children}) =>{
    return(
        <div>
            <h1>this is my first frame to use in my site</h1>
            {children}
        </div>
    );
}
export default Frame;